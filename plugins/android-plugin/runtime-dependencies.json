{
  "_comment": "Runtime plugin dependencies for android-plugin, aggregated by pipeline-orchestrator Step 0a. Only marketplace-plugin deps the core understands belong here. Android CLI is a SYSTEM tool, not a plugin — it is checked entirely inside android-plugin (hooks/android-cli-check.sh), never by the core. So this stays empty.",
  "dependencies": []
}
