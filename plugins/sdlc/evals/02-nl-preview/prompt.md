---
max_turns: 12
timeout_seconds: 300
allowed_tools: [Skill, "Bash(node:*)", "Bash(jq:*)"]
model: sonnet
runs: 3
---
Before I spend anything: preview what the SDLC pipeline would do for 'After an action is logged, show a confirmation snackbar with an Undo action that removes the just-logged entry.' Dry run only — don't run any phases.
