---
pr: 62
date: 2026-07-23
author: Nuclominus
type: docs
plugins: []
roadmap: null
files_changed: 5
---

# PR #62 — plan-derived tracks + read-only board

> `docs` · merged 2026-07-23 · by @Nuclominus

## Summary

Absorbs the *Roadmap Development Plan* into the Second Brain vault and adds a rendered, read-only board view.

## Changed areas

- Repo-level change (no plugin under `plugins/` touched). Adds the `roadmap/` render tooling
  (`roadmap/generate.mjs` + `roadmap/index.html`) and expands the vault planning notes.

## Decisions & rationale

- **The vault roadmap stays the single source of truth; the board is a derived, read-only view.**
  `roadmap/generate.mjs` parses the [[planning/roadmap]] table and rewrites only the SEED block
  between `<<SEED-START>>`/`<<SEED-END>>` markers in `roadmap/index.html` — curated card prose and
  priority flags live in the generator, so re-running never loses them and never hand-edits the HTML.
  No ADR: this is presentation tooling over existing SSOT, consistent with the dep-free / regenerate-
  don't-hand-edit discipline used elsewhere in the vault.

## Planning

- Absorbs the **Roadmap Development Plan** into [[planning/roadmap]] and [[planning/backlog]] —
  seeding the next evolutionary phase (goal: complex-task completion ~70% → 90%) as new tracks:
  **E6–E8** (cache/cost), **F1–F2** (time/parallelism), **G1–G2** (quality/autonomy), with
  G1 + E8 flagged highest-ROI.

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
