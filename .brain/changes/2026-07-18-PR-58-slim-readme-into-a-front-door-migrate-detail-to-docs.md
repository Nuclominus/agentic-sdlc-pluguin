---
pr: 58
date: 2026-07-18
author: Nuclominus
type: docs
plugins: [sdlc]
roadmap: null
files_changed: 10
---

# PR #58 — slim README into a front door, migrate detail to docs/

> `docs` · merged 2026-07-18 · by @Nuclominus

## Summary

`README.md` had grown to **618 lines**, mixing three audiences (first-time installer, day-to-day
operator, plugin author). This slims it to a **~115-line front door** — title, quickstart, a
Documentation index, the Commands and Available-Plugins tables, trimmed Security/Vault pointers,
requirements, and license — and moves every deep topic behind a link, extending the repo's
established pointer-stub pattern (`ARCHITECTURE.md` / `CORE-TODO.md` → `.brain/`).

Detail migrated into topic-per-file pages under `docs/` (user-facing, distinct from this `.brain/`
engineering SSOT):

- `docs/RECIPES.md` — dynamic workflow recipes (built-in recipes, control-flow shapes, auto-selection, project-local recipes).
- `docs/COST-AND-MODELS.md` — model-tier enforcement + cost optimization + reports / rollup / AAR.
- `docs/CONFIGURATION.md` — `.claude/sdlc.local.yaml` overrides + Project Extension Manifest.
- `docs/INSTALLATION.md` — step-by-step install + requirements.
- `docs/README.md` — a docs index.

`docs/WORKFLOW.md` absorbed the Stack Provider Pattern prose, priority / detection tables, framework
provider pattern, vanilla fallback, and composition examples (beside its existing Mermaid diagrams).
`CONTRIBUTING.md` absorbed both "Adding a plugin" sections with their `manifest.yaml` examples,
schema validation, and verify-locally block (deduped, not re-copied). A light one-emoji-per-page
accent was added to doc titles and the index tables. No content was lost — every prior README
heading maps to a new home.

## Changed areas

- [[components/sdlc]] — `plugins/sdlc/README.md`: two stale inbound links repointed to the new
  `docs/COST-AND-MODELS.md` (model enforcement) and `docs/CONFIGURATION.md` (local overrides) pages.
- Repo-root user documentation: `README.md`, `CONTRIBUTING.md`, and the new/updated `docs/` page set.

## Decisions & rationale

- **Target = `docs/` folder, not a GitHub Wiki** — versioned with code, reviewable in PRs, present
  in every clone; the repo has no Wiki and one would drift from the code.
- **Dedupe & link, don't re-copy** — content already living in `CONTRIBUTING.md` or `.brain/` is
  linked once; each user-facing topic gets exactly one canonical home (no third copy to drift).
- **Topic-per-file** granularity, and emoji confined to H1 titles + table cells so existing deep
  anchors (e.g. `README#quickstart`) keep working. Lightweight and reversible — no ADR warranted.

## Planning

- No roadmap item — documentation hygiene, not a roadmap feature.

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
