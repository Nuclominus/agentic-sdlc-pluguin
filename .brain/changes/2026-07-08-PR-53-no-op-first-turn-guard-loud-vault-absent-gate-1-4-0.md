---
pr: 53
date: 2026-07-08
author: Nuclominus
type: fix
plugins: [android-foundation]
roadmap: null
files_changed: 8
---

# PR #53 — no-op first-turn guard + loud vault-absent gate (1.4.0)

> `fix` · merged 2026-07-08 · by @Nuclominus

## Summary

Two workflow findings from the AAR of run `brain-rudderstack-phase-c`. **F1 (no-op first turns):**
the BA and developer first spawns returned `tool_uses:0` (~24.5K tokens, 9s) and forced a
SendMessage resume, because the agents opened by reasoning about skill selection / vault reads
instead of acting. **F2 (vault never inherited by worktrees):** an untracked `.obsidian-vault` is not
checked out into `git worktree`s, so phases shipped with zero docs while the DoD gate silently
`exit 0`.

## Changed areas

- [[components/android-foundation]] — F1: a "first action MUST be a tool call; vault-absent → skip"
  guard in the shared `rules/documentation.md` (reaches every agent via `loaded_by`) plus the
  `android-ba` / `android-developer` bodies. F2: `vault/scripts/validate-docs.mjs` +
  `check-docs-sync.sh` now warn and drop a `_vault-pending.md` breadcrumb instead of a silent pass;
  the `android-docs` DoD, `android-reviewer` enforcement, and `manage-vault` preflight treat a
  missing/untracked vault as a visible blocker.

## Decisions & rationale

- Applies AAR findings F1/F2 from the same run whose cost plumbing PR #52 fixed. No ADR — these are
  workflow-prompt/guardrail fixes, not an architecture decision. The vault stays an **optional**
  module, so the gate is loud but non-fatal (a project without a vault is never blocked). The
  consumer-side root cause (committing a project's `.obsidian-vault` submodule so worktrees inherit
  it) is a repo-owner action, tracked separately.

## Planning

- _No roadmap item tagged (workflow-quality fix)._

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
