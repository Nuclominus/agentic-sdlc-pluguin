---
pr: 204
date: 2026-09-22
author: Nuclominus
type: fix
plugins: [sdlc]
roadmap: null
files_changed: 15
---

# PR #204 — doctor, init and list-stacks are reachable on Antigravity

> `fix` · merged 2026-09-22 · by @Nuclominus

## Summary

Antigravity's `agy plugin install`/`validate` reports `commands: 11 processed
(converted to skills)`, and the Track J emitter's design comment took that at
face value: Antigravity does the command→skill conversion itself, so nothing
more was needed. Measured against the real installed package on agy 1.2.8,
that claim is false — `agy -p "/skills"` proves only the pre-existing
`skills/*/SKILL.md` directories (`aar`, `create-pluguin`,
`pipeline-orchestrator`) were ever invocable. The 11 `commands/*.md` files
get copied byte-identical into the install, but nothing on the host maps
them to a callable skill or slash command. A live pipeline run on
`parlor-android` hit this directly: the resolver's own legacy-location
`WARN:` told the user to run `/sdlc-doctor`, a command that could not be
invoked on that host at all.

## Changed areas

- [[components/sdlc]]

## Decisions & rationale

- Extends [[decisions/ADR-0029-one-ssot-emitted-host-packages]] (#202): that ADR's "no translation
  layer" premise assumed the host converts `commands/*.md` into invocable skills on install. This
  PR corrects that assumption in the emitter's own design comment and supplies the missing piece
  by hand — `doctor`, `init` and `list-stacks` now exist as real `skills/<name>/SKILL.md` files,
  the same shape `start`, `aar` and `create-pluguin` already used, with the Claude-only command
  thinned to a delegator. `batch`, `extension`, `model-config`, `workflow-config`,
  `security-init` and `report` remain unreachable on Antigravity — noted as follow-up work, not a
  regression introduced here. `security-init` targets a plugin, `security-guidance`, that does not
  exist in this marketplace on any host at all — a separate, pre-existing issue.

## Planning

- [[planning/j1-multi-host]] — Track J follow-up: closes part of the Antigravity command-reachability
  gap this note's own "Decisions" section names. The remaining six commands are the next slice.

---
_Auto-generated by `tools/brain-sync`. Frontmatter and the index are machine-owned; every prose section, Summary included, is meant to be enriched — but `sync --pr` rewrites the whole file, so enrich after running it._
