---
pr: 212
date: 2026-09-23
author: Nuclominus
type: feat
plugins: []
roadmap: null
files_changed: 4
---

# PR #212 — generic namespaced doc-reference check, replacing the one-off AAR CI step

> `feat` · merged 2026-09-23 · by @Nuclominus

## Summary

- New `tools/sdlc-lint/lib/doc-refs.mjs`: `checkDocRefs({ repoRoot })` scans README/CONTRIBUTING/`docs/**/*.md` for backtick `` `<plugin>:<name>` `` references and verifies each resolves to a real agent/skill/command file — generalizing CI's one-off "AAR reference integrity" grep (written for exactly one historical rename, `android-workflow:aar` → `sdlc:aar`) into a reusable check.
- Wired into `sdlc-lint all`/`sdlc-lint doc-refs`; the bespoke CI grep block is removed (the four `test -f` presence assertions it also did are kept — those aren't reference-drift, they're file-existence checks this module doesn't cover).
- `docs/superpowers/**` is excluded from the scan (mirroring the old grep's own `--exclude-dir=superpowers`): that directory holds historical plan/spec narrative that legitimately references pre-rename names as part of its own frozen record, not living documentation.

## Changed areas

- _Repo-level change (no plugin under `plugins/` touched)._

## Decisions & rationale

- No standalone ADR: this is a mechanical generalization of an existing, already-decided CI
  check (the one-off AAR grep), not a new policy. The one judgment call — excluding
  `docs/superpowers/**` — mirrors the exemption the grep it replaces already had, so it
  preserves existing behavior rather than introducing a new one.

## Planning

- Part of the `claude-sdlc`-inspired hardening series (PRs #208–#213). See
  [[changes/2026-09-23-PR-208-branch-and-staleness-preflight-before-resume]] for the series
  overview; no single roadmap item tracks it.

---
_Auto-generated by `tools/brain-sync`. Frontmatter and the index are machine-owned; every prose section, Summary included, is meant to be enriched — but `sync --pr` rewrites the whole file, so enrich after running it._
