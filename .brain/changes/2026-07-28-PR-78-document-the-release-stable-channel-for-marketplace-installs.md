---
pr: 78
date: 2026-07-28
author: Nuclominus
type: docs
plugins: []
roadmap: null
files_changed: 3
---

# PR #78 — document the @release stable channel for marketplace installs

> `docs` · merged 2026-07-28 · by @Nuclominus

## Summary

- A `release` branch now exists (cut from `develop` at c1e3ac1) as the stable install channel for the marketplace.
- README Quickstart now recommends `/plugin marketplace add Nuclominus/Agentic-SDLC-Pluguin@release` and explains what the suffix does.
- `docs/INSTALLATION.md` documents both channels (stable `@release` vs bleeding-edge `develop`) and the update semantics: a marketplace added with a branch ref keeps updating from that ref.
- **New `/release` project command** (`.claude/commands/release.md`) automates cutting a release: preflight (nothing-to-ship / divergence guards, never force-pushes), version bump `chore(release): vX.Y.Z` on `develop` via a temp worktree, fast-forward of `release`, and a `vX.Y.Z` tag. Includes a warn-only gate for plugins whose content changed since the last release without a `plugin.json` version bump.

## Changed areas

- _Repo-level change (no plugin under `plugins/` touched)._

## Decisions & rationale

- _Enrich: record or link the decision behind this change, e.g. `decisions/ADR-XXXX`._

## Planning

- _No roadmap item tagged._

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
