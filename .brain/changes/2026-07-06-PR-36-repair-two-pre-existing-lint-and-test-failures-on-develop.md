---
pr: 36
date: 2026-07-06
author: Nuclominus
type: fix
plugins: []
roadmap: null
files_changed: 2
---

# PR #36 — repair two pre-existing lint-and-test failures on develop

> `fix` · merged 2026-07-06 · by @Nuclominus

## Summary

The `lint-and-test` CI job had been red on `develop` due to **two independent, pre-existing failures** (unrelated to any feature work); surfaced while running CI on #35. This PR fixes both so the whole repo goes green.

1. **Stale `load.test.mjs` frameworks snapshot** — the *"loadManifests splits foundations and frameworks"* test pinned the list to `['dagger','retrofit','room']`, but the workmanager-plugin framework (added in #29) makes the loaded list include `'workmanager'`. Updated the expected array → `node --test` passes 82/82.
2. **`AAR reference integrity` false positive** — the step greps for the dead `android-workflow:aar` identifier across `*.md`/`*.yaml`. A change note under `.brain/changes/` (the #34 vault backfill's PR-27 note) legitimately quotes that old name in its historical narrative. Added `--exclude-dir=.brain` to the grep, mirroring the existing `docs/superpowers/` exclusion carried for the same reason.

## Changed areas

- Repo-level CI/test hygiene (no plugin under `plugins/` touched): `.github/workflows/ci.yml`, `tools/sdlc-lint/test/load.test.mjs`.

## Decisions & rationale

- No ADR — CI/test hygiene, not a design decision. Chose to **exclude `.brain/` from the dead-identifier grep** rather than rephrase the historical note: the vault's `changes/` are immutable historical records that may legitimately quote deprecated identifiers, so the check should target active config only (same rationale as the pre-existing `superpowers` exclusion).

## Planning

- No roadmap item — unblocks CI for `develop` and for the session-recorder feature PR (#35).

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
