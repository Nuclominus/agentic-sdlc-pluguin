---
pr: 144
date: 2026-09-07
author: Nuclominus
type: docs
plugins: []
roadmap: null
files_changed: 4
---

# PR #144 — reconcile the board with what shipped; trim the README to five next steps

> `docs` · merged 2026-09-07 · by @Nuclominus

## Summary

Reconciles the roadmap with what the repository actually contains, and replaces the README's roadmap
screenshot with five next steps.

## Changed areas

- _Repo-level change (no plugin under `plugins/` touched)._

Three rows carried a status the repository no longer supported, and two "unmeasured" items finally
had a real run to measure against.

**E6 closed as achieved, not built.** Its goal was a byte-identical stable prefix for maximum
prompt-cache hits. The #142 validation run reports `cache_hit_ratio: 1.0` — 17.5M cached input
tokens against 577 uncached, across 11 dispatches, *with* the new `role_expertise` blocks in the
prefix. Nothing left to build for.

**E8 was half-done all along.** `/sdlc:batch` ships the throughput half — decompose scope, detect
file conflicts, dispatch worktree-isolated pipelines in parallel — but not the init-cost
amortization E8 was specced for, since each pipeline still pays its own start window. The row now
says which half is which.

**B3 dropped.** It read `(planned)` with "next Track B item — scope TBD" for the life of the board
while tracks E–I absorbed everything it might have covered. A row that reads as queued work but
names none is worse than no row.

### Two measurements that change what to do next

**E2's behavioural DoD misses on its first real downstream run.** Target: `peak_prefix_tokens` under
60k, down from a 101k baseline. Recorded per phase: documentation 44k, security 53k,
business_analysis 60k, review 77k, qa 80k, development 121k, **test 200k**. One run is not a verdict,
but read beside the A/B in [[planning/backlog]] where *both* arms met `<60k` and the threshold was
judged not to discriminate, the honest reading is that the metric tracks task size at least as much
as read discipline. The DoD needs replacing before it can be passed or failed.

**The start-window number from that run is an artefact, not a result.** `sdlc-lint start-window`
reports 9 API calls for Steps 0→1d, apparently flat against the 9-call baseline — but the operator
ran `/sdlc:start --dry-run` and the real `/sdlc:start` in one session, so both resolve invocations
fall inside the measured window. Recorded with the instruction that the next attempt use a clean
session, so nobody later mistakes it for evidence. The H4 gate does advance by one: this is the
first run carrying `plugin_version: 2.0.0`, at 100% on all five live contracts.

## Decisions & rationale

- No new ADR. The status changes are evidence applied to existing ones:
  [[decisions/ADR-0008-read-discipline-contract]] (E2's DoD, now measured and missed) and
  [[decisions/ADR-0019-the-run-start-is-one-command]] (its DoD, still unmeasured — the one attempt
  was confounded).
- The roadmap pointed at a repo-root `Roadmap Development Plan.md` that no longer exists; it was
  absorbed into [[planning/backlog]] when the vault became the SSOT. The `§` references stay, since
  the backlog entries still carry them, now with a note saying what they point at.

## Planning

- Updates [[planning/roadmap]] itself — the statuses above, plus F2's note that its mechanism (the
  `LOC_TOUCHED` skip-rules engine, #119) already shipped and only the recipe is missing.
- `roadmap/index.html` regenerated from the table: 23 items, done 14 / progress 2 / review 1 /
  todo 6. `generate.mjs` gains the `I1` description it never had — that card had been rendering
  blank — and loses B3's now-dead entries.
- **README:** the board screenshot is gone. It had grown into a wall of cards unreadable at page
  width, and being a static capture it went stale between releases. Replaced by five next steps
  written for someone who does not follow the project — 17–24 words each, no ADR numbers, no track
  letters, no metric names — over a link to the generated board. The evidence stays here in the
  vault, which is where a reader who does follow the project looks.

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
