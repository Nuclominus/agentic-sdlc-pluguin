---
pr: 44
date: 2026-07-07
author: Nuclominus
type: fix
plugins: [android-foundation, sdlc]
roadmap: null
files_changed: 15
---

# PR #44 — apply AAR plugin-cache findings to plugin source

> `fix` · merged 2026-07-07 · by @Nuclominus

## Summary

Reconciles the `brain-rudderstack-phase-b` After Action Review's 7 findings to their **real** source anchors and fixes them in **plugin source** (the AAR targeted the plugin *cache*, whose edits are clobbered on update). Several findings were misattributed in the report (wrong step numbers, a non-existent "template SKILL.md", a `notes` field that doesn't exist) — each fix lands where it actually applies, not where the report literally said.

The two systemic fixes: **(1) telemetry no longer zeroes all tokens** — the harness result envelope exposes only an aggregate `subagent_tokens` count, not the split input/output/cached triple the orchestrator's Step 3d-1 expected, so estimation always fired and the dashboard read all-zero with a misleading zero cache ratio; Step 3d-1 now captures the aggregate verbatim (`usage_source: subagent_aggregate`), Step 5 sums `total_subagent_tokens` and reports `cache_hit_ratio: null` when genuinely unknown, and `tools/aar/metrics.mjs` surfaces it. **(2) crash recovery is defined and correctly labelled** — the workflow now resumes the same agent in-session before spawning a fresh one, and records which mechanism ran in a new per-phase `recovery` field. New checkpoint fields (`subagent_tokens`, `tool_uses`, `duration_ms`, the `subagent_aggregate` usage source, `recovery`) are registered in `schemas/checkpoint.schema.json`. Smaller corrections from the same review: worktree-first workspace resolution (orchestrator Step 2), point-of-use Skill self-checks + a skills-matrix row-label fix in the android agents, a documented docs-phase `haiku→sonnet` escalation, and de-hardcoded logging rules ("the project's logger, Kermit if present").

Released as `sdlc` 1.6.0 + `android-foundation` 1.3.0 (marketplace 1.6.0).

## Changed areas

- [[components/sdlc]] — orchestrator Step 3d-1/Step 5 telemetry assembly, `tools/aar/metrics.mjs` dashboard, `schemas/checkpoint.schema.json`.
- [[components/android-foundation]] — workflow crash-recovery rule + docs-phase model note, BA/developer agent Skill self-checks, de-hardcoded logging rules.
- [[architecture/pipeline-orchestrator]] — telemetry envelope handling now has three shapes (reported / subagent_aggregate / estimated).

## Decisions & rationale

- Implements [[decisions/ADR-0004-aggregate-token-telemetry-crash-recovery]] — capture the harness's aggregate token count verbatim rather than fabricating a split or discarding it to a char/4 estimate; and prefer in-session `SendMessage` resume over a fresh agent on a mid-run crash, recording the mechanism so cost attribution stays honest.
- Rejected in the ADR: fabricating an input/output split (invents cost/cache fiction), keeping the char/4 estimate (throws away real measured usage), and overloading the existing `origin` field (conflates cross-session checkpoint resume with in-run crash recovery).

## Planning

- No new roadmap track. Hardens the already-shipped **C1 — AAR learning cycle `/sdlc:aar`** (#27) in [[planning/roadmap]]: the AAR dashboard this fix repairs is C1's own consumer, which had been reporting all-zero tokens.

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
