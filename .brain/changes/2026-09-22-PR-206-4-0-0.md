---
pr: 206
date: 2026-09-22
author: Nuclominus
type: chore
plugins: [android-foundation, sdlc]
roadmap: null
files_changed: 6
---

# PR #206 — 4.0.0

> `chore` · merged 2026-09-22 · by @Nuclominus

## Summary

Version bump to cut a new release: `sdlc` `2.5.0` → `3.0.0`, `android-foundation` `3.0.0` →
`3.0.1`, marketplace `3.0.0` → `4.0.0`.

## Changed areas

- [[components/android-foundation]]
- [[components/sdlc]]

## Decisions & rationale

- Major bump on the marketplace and on `sdlc` is driven by
  [[decisions/ADR-0030-the-project-sdlc-directory-is-ours]] (a breaking config-location change,
  `.claude/` → `.sdlc/`, no alias layer) and by [[decisions/ADR-0029-one-ssot-emitted-host-packages]]
  (#202's multi-host emission pipeline, this cycle's headline feature). `android-foundation` gets
  only a patch bump — its one change since `3.0.0` was #200's dependency-source correction.
- Converts `CHANGELOG.md`'s `[Unreleased]` section into the dated `4.0.0` entry, backfilling the
  `### Added` bullets for #202 and #204 it was still missing, and opens a fresh empty
  `[Unreleased]` above it.

## Planning

- [[planning/j1-multi-host]] — this release is what makes Track J's Antigravity port (and the
  `doctor`/`init`/`list-stacks` follow-up, #204) installable outside a development checkout.

---
_Auto-generated by `tools/brain-sync`. Frontmatter and the index are machine-owned; every prose section, Summary included, is meant to be enriched — but `sync --pr` rewrites the whole file, so enrich after running it._
