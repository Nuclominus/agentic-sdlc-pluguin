---
pr: 193
date: 2026-09-21
author: Nuclominus
type: chore
plugins: [sdlc]
roadmap: null
files_changed: 2
---

# PR #193 — prepare the 3.0.0 release notes and bump sdlc

> `chore` · merged 2026-09-21 · by @Nuclominus

## Summary

Release prep for v3.0.0, surfaced by a `/release` preflight that was **cancelled rather than
pushed** — the command's step 4 confirmation exists for exactly this, and it earned its keep here.

The `## [3.0.0]` changelog section had been written inside #190 and described only #190. But the
release ships **24 PRs** since v2.0.0, so ten substantive ones had no entry at all: the dry-run eval
suite and its ninth case, path-loaded plugin self-discovery, prose recipe resolution, the
unknown-recipe warning, preview triggers, the `--resume --dry-run` cost fix, ADR-0025, the
Summary-is-enrichable rule change, and the docs refresh. This adds `Added` / `Fixed` / `Changed`
sections covering them, matching the shape of `[2.0.0]`.

It also corrects a claim that would have shipped verbatim in the public GitHub release notes: the
marketplace goes from **11 entries to 4**, not "9 to 3". Both figures were checked against the refs
themselves (`origin/release` carries 11, `origin/develop` carries 4) rather than against the prose
that asserted them — which is how the error survived being written, reviewed and merged.

`plugins/sdlc` moves `2.4.1` → `2.5.0`. Its content changed substantially in this window — the
resolver, the manifest schema, two new lint verbs, the migrate tooling, `create-pluguin`, the
orchestrator skill and the eval suite — while its version stood still, which `/release`'s per-plugin
gate flags as warn-only: installed copies of an explicitly-versioned plugin may not pick up an
update whose version never moved. That gate is deliberately warn-only because the fix does not
belong to the release command; `/release` moves the marketplace version alone, so a plugin's own
version is a normal PR. This is that PR.

## Changed areas

- [[components/sdlc]] — plugin version `2.4.1` → `2.5.0`.
- `CHANGELOG.md` — the `[3.0.0]` section, now covering the whole release window rather than its
  last PR.

## Decisions & rationale

- No ADR: this records work already decided elsewhere. The release it prepares implements
  [[decisions/ADR-0026-embed-framework-providers-in-the-foundation]], and the entries added to the
  changelog trace back to [[decisions/ADR-0024-naming-a-recipe-is-an-explicit-request]],
  [[decisions/ADR-0025-a-branch-switch-is-a-file-operation]] and
  [[decisions/ADR-0013-agent-memory-mirrors-into-the-vault]].
- The operative principle is procedural and already written down in `/release`: a release command
  moves refs, it does not author the content it ships. A changelog section that describes one PR
  out of twenty-four is a content problem, so it is fixed in a PR and the release is re-run
  afterwards.

## Planning

- No roadmap item — release mechanics rather than tracked work. It unblocks the v3.0.0 cut, which
  ships [[planning/c3-embed-framework-providers]] to the `@release` install channel.

---
_Auto-generated by `tools/brain-sync`. Frontmatter and the index are machine-owned; every prose section, Summary included, is meant to be enriched — but `sync --pr` rewrites the whole file, so enrich after running it._
