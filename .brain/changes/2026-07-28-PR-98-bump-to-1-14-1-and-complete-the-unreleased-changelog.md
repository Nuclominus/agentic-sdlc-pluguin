---
pr: 98
date: 2026-07-28
author: Nuclominus
type: chore
plugins: [sdlc]
roadmap: null
files_changed: 2
---

# PR #98 — bump to 1.14.1 and complete the unreleased changelog

> `chore` · merged 2026-07-28 · by @Nuclominus

## Summary

Pre-release housekeeping. `/release patch` was started and **cancelled at the confirmation step** — nothing was pushed, no ref moved — because its per-plugin gate warned.

## Changed areas

- [[components/sdlc]] — `.claude-plugin/plugin.json` `1.14.0` → `1.14.1`. #92 had changed five files
  inside the shipped payload (`skills/pipeline-orchestrator/SKILL.md`, both `tools/report/*` and
  both `tools/usage/*`) while the plugin's own version stood still.
- `CHANGELOG.md` — `[Unreleased]` gained the `sdlc 1.14.0 → 1.14.1` lead line and the two entries it
  was missing: the `reindex` verb (#96) and Track H (#93).

## Decisions & rationale

No ADR — release housekeeping, but two things are worth keeping on the record.

- **The gate earned its keep on its first real run.** `/release`'s per-plugin version check (added
  in #90) is warn-only by design, and it fired correctly: a marketplace version can advance to
  `1.11.2` while an installed copy of an explicitly-versioned plugin never picks up the update,
  because *that plugin's* version never moved. The cost-record fixes would have shipped in name
  only. The release was cancelled at the confirmation step with no ref moved, and the fix landed
  here as a normal PR — which is exactly the separation `/release` step 2 prescribes: the command
  warns, it does not edit plugin versions itself.
- **An incomplete `[Unreleased]` is an incomplete release.** `/release` promotes that section
  verbatim into the GitHub release notes, so a change absent from it is invisible to every reader
  of the release — the changelog is not a summary written afterwards, it is the artifact.

## Planning

No roadmap track — release housekeeping.

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
