---
pr: 213
date: 2026-09-23
author: Nuclominus
type: docs
plugins: [sdlc]
roadmap: null
files_changed: 4
---

# PR #213 — make the quadratic-cost-in-dispatch-length heuristic explicit

> `docs` · merged 2026-09-23 · by @Nuclominus

## Summary

- `plugins/sdlc/skills/aar/gather.md`: new "Cost-shape heuristic" section — dispatch cost is not linear in phase count; a phase-level cut removes that phase's own cost AND the marginal cost every later phase paid to carry its context forward, while re-tiering a phase's model only saves that phase's own delta.
- `plugins/sdlc/skills/aar/report.md` §5: the "Highest-leverage summary" now requires the analyst to rank a viable phase-level cut above a same-phase model-tier downgrade, and to state which kind of change ("cut" vs. "re-tier") a finding is.
- Pure documentation — no code, no tests, no ADR (this phase has no architectural trade-off, per the plan's Global Constraints).
- Last PR in the `claude-sdlc`-inspired hardening series (#208–#213), closing it out.

## Changed areas

- [[components/sdlc]]

## Decisions & rationale

- No ADR: a prose-only correction to the AAR analyst's cost model, not an architectural
  decision — the plan's own Global Constraints scoped ADRs to Phases 1-4 (resume preflight,
  commit-hygiene hook, config-protection hook, phase-boundary lint feedback) precisely because
  those introduced new gating behavior; this phase and the doc-refs-check phase (#212) did not.

## Planning

- Closes the `claude-sdlc`-inspired hardening series (PRs #208–#213): resume safety
  ([[decisions/ADR-0031-resume-preflight-gates]]), commit-hygiene hook
  ([[decisions/ADR-0032-commit-hygiene-is-a-deterministic-hook]]), config protection during a
  run ([[decisions/ADR-0033-lint-config-edits-are-gated-during-a-run]]), phase-boundary lint
  feedback ([[decisions/ADR-0034-phase-boundary-lint-feedback-never-blocks]]), the generic
  doc-reference check (#212), and this AAR cost-heuristic doc fix. No single roadmap item
  tracks the series.

---
_Auto-generated by `tools/brain-sync`. Frontmatter and the index are machine-owned; every prose section, Summary included, is meant to be enriched — but `sync --pr` rewrites the whole file, so enrich after running it._
