---
pr: 80
date: 2026-07-27
author: Nuclominus
type: chore
plugins: [android-foundation, sdlc]
roadmap: null
files_changed: 2
---

# PR #80 — sdlc 1.11.0, android-foundation 1.5.0 for the G1 release

> `chore` · merged 2026-07-27 · by @Nuclominus

## Summary

Both plugins shipped G1 content in #77 with versions unchanged; installed copies cache by version and would not refresh. Minor bumps (G1 is a feature) so the upcoming marketplace release actually reaches existing installs.

## Changed areas

Two `.claude-plugin/plugin.json` manifests, nothing else — no behavior change in this PR.

- [[components/sdlc]] — `1.10.1` → **`1.11.0`**. Carries the G1 orchestrator work from #77: the
  `heal:` workflow primitive, step 3e-heal, the `heal_attempts_used` / `heal_status` checkpoint
  fields, and the raised `analysis` / `hotfix` cost caps.
- [[components/android-foundation]] — `1.4.0` → **`1.5.0`**. Carries the android workflow recipes
  that actually guard `development` / `qa` with compile-only `heal_checks`.

Shipped to the stable channel by marketplace release **v1.10.0** (`e355071`).

## Decisions & rationale

- Implements the delivery half of [[decisions/ADR-0010-self-healing-micro-loop]] — #77 landed the
  feature, this PR is what makes it reachable.
- **Why a version bump was required at all:** installed plugin copies are cached **by version**. A
  PR that changes plugin *content* without touching `plugin.json` is invisible to every existing
  install — the marketplace serves the cached tree until the version string moves. #77 shipped 32
  files of G1 behavior under unchanged versions, so on merge day the feature existed on `develop`
  and nowhere else. This is a standing trap for any content-only PR under `plugins/`, not a
  one-off.
- **Why minor, not patch:** G1 adds a capability (a new `heal:` primitive and new checkpoint
  fields), so semver minor. The `/release` command proposed in #78 (open at time of writing) adds a
  warn-only gate for exactly this omission — plugins whose content changed since the last release
  without a `plugin.json` bump.

## Planning

- Advances [[planning/roadmap]] **Track G — quality & autonomy**: G1 (self-healing compiler/lint
  micro-loops) is marked `done` against #77; this PR is what delivers it to installs.

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
