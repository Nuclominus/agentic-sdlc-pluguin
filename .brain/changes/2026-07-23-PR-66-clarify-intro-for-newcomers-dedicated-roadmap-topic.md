---
pr: 66
date: 2026-07-23
author: Nuclominus
type: docs
plugins: []
roadmap: null
files_changed: 1
---

# PR #66 — clarify intro for newcomers + dedicated roadmap topic

> `docs` · merged 2026-07-23 · by @Nuclominus

## Summary

Reworks the top of `README.md` so a first-time reader can tell what the marketplace does before
hitting any internals:

- **Intro rewritten in plain language** — dropped the version tag (`v0.5.0`) and the internal
  vocabulary ("Stack Provider Pattern", model tiering, `manifest.yaml`/`kind:` mechanics). It now
  states the outcome (describe a feature → a team of agents runs the full cycle) and the single
  organizing idea (a fixed core drives the pipeline; foundation + framework plugins add
  platform/library knowledge and auto-activate).
- **Roadmap promoted to its own `## Roadmap` topic** — pulled the roadmap sentence out of the
  Documentation section. The new topic carries a small top-3 priority table plus the board
  screenshot, which now links to the full-size image (opens in a new tab).

Docs-only; no plugin, agent, or tool behavior changed.

## Changed areas

- `README.md` — intro paragraphs + new `## Roadmap` section (README is the "front door" per the
  Documentation section's own framing).
- Priority table sourced from [[planning/roadmap]] (Track E in-progress; G1 + E8 as the plan's
  highest-ROI next steps) — a rendered mirror, not a new source of truth.

## Decisions & rationale

- No ADR: this is presentational polish of the front door, not an architecture or process decision.
  The engineering SSOT is unchanged — [[planning/roadmap]] remains authoritative; the README table
  is a convenience snapshot that points readers at it.

## Planning

- No roadmap item advanced or added; the roadmap is referenced by the README, not changed by it.
  See [[planning/roadmap]].

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
