---
pr: 40
date: 2026-07-06
author: Nuclominus
type: chore
plugins: [sdlc]
roadmap: null
files_changed: 4
---

# PR #40 — sdlc 1.5.0 + marketplace 1.5.0

> `chore` · merged 2026-07-06 · by @Nuclominus

## Summary

Cuts **v1.5.0**. Bumps the only plugin that changed since its last version — **`sdlc` 1.4.0 → 1.5.0** (session-recorder closing agent + measured run clock, #35) — and the marketplace to match. Other plugins are unchanged (per-plugin versioning; versions intentionally diverge). The tag `v1.5.0` and its GitHub release also **catch up** the `sdlc` work that shipped to `develop` since **v1.2.0** without a cut release (the intermediate 1.3.0/1.4.0 version steps were never tagged): AAR cycle (#27), `--resume` (#25), HTML report (#26), rollup (#28), WorkManager (#29), dry-run + caps (#21), workflow auto-selection (#20), project-local workflows (#22), `sdlc-lint` CI (#23), and the Second Brain vault (#31–#34).

## Changed areas

- [[components/sdlc]] — version 1.5.0.
- [[releases/_moc-releases]] — tag list seeded; `CHANGELOG.md` `[1.5.0]` section added.

## Decisions & rationale

- No ADR — a release chore, not a design decision. Scope call: bump **only the changed plugin** (`sdlc`) plus the marketplace mirror, per the current per-plugin convention (versions diverge on purpose), rather than a uniform all-plugin bump. Release notes cover everything since the last *actual* release (v1.2.0), reconciling the untagged 1.3.0/1.4.0 drift.
- Ships the design in [[decisions/ADR-0003-session-recorder-run-journal]] to users.

## Planning

- No roadmap track. Releases the run-observability line — [[planning/roadmap]] **B2** (`/sdlc:report`) and **D** (HTML report) — now backed by measured timing, plus the new cumulative run journal.

---
_Auto-generated by `tools/brain-sync`. Frontmatter is machine-owned; prose below "Summary" is safe to enrich._
