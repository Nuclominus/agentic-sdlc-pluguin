---
status: planned
---

# Backlog

> Open items migrated from `CORE-TODO.md`. Promote to a [[planning/roadmap]] track when scheduled.

No open items migrated; see [[planning/roadmap]]. Every `CORE-TODO.md` section is marked
`*(DONE)*` / `*(DONE — verified)*` / `*(DONE — no core change)*`, and Section 6
(`Orchestrator support for the rich Android pipeline`, tagged `*(PARTLY DONE)*`) lists only
`DONE` sub-items plus one deliberately `DROPPED` item (`android:` block in `sdlc.local.yaml`) —
no still-open remainder to carry forward.
