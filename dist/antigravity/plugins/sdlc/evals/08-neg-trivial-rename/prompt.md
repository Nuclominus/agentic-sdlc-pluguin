---
max_turns: 5
timeout_seconds: 120
allowed_tools: [Skill]
model: sonnet
runs: 3
---
Rename `cnt` to `count` in this snippet and show me the result: `fun total(xs: List<Int>): Int { var cnt = 0; for (x in xs) cnt += x; return cnt }`
